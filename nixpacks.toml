[start]
cmd = "python manage.py migrate --noinput --verbosity 2 && python manage.py collectstatic --noinput && python manage.py ensure_superuser && gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 3"
